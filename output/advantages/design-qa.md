# 四大核心優勢插圖驗證

final result: passed

日期：2026-09-06。範圍是將使用者選定的第一組玻璃插圖修正版放入既有首頁輪播：台灣時事感測、三方檢視、門檻開關、鏈上指紋。

## 來源與證據

- 視覺來源：`output/advantages/reference.png`（1774 × 887）。原檔 `/Users/ritali/.codex/generated_images/01a072cd-c53c-7ac1-8277-4ff9a21698ed/exec-5effacc1-28c2-4925-9d86-900965705e52.png`。
- 實作：<http://localhost:3000/#advantages>。
- 全景截圖：`output/advantages/desktop-light-1.png` 至 `desktop-light-4.png`、`desktop-dark-3.png`。
- 插圖並排比對：`output/advantages/comparison.png`。四組依左上、右上、左下、右下排列；每組左為參考面板，右為瀏覽器圖卡。
- 手機證據：`output/advantages/mobile-light-zh.png`、`mobile-dark-en.png`。
- 素材與完整生成提示詞索引：`output/advantages/README.md`。

來源是插圖選稿板，並非新的網頁版型。實作保留原輪播雙欄、文字、順序與控制項；選稿板的四欄與圖下標籤不作為網頁版型套用。

桌面 viewport 與 PNG 均為 1440 × 1050，一個 CSS px 對應一個 PNG px。圖像區約 584.8 × 731.5 CSS px。比較時依 DOM 座標截取約 585 × 731 px，來源面板約 425 × 644 px，各自等比例縮至 640 px 高；沒有拉伸。對照圖 1980 × 1320，四組圖卡的玻璃邊緣、台灣輪廓、儀表和指紋均可檢查。亦已檢查全景截圖中的文字、間距與按鈕。正式素材增加場景留白以適應既有桌面 4:5 卡框。

手機 viewport 與 PNG 均為 390 × 844，圖像區 348 × 435.5 CSS px，採完整直幅。實測 document 寬度為 390 px，無橫向溢出。較長內容與控制項可垂直捲動閱讀及操作。

## 發現與比對紀錄

- 沒有未解決的 P0／P1／P2 問題。
- 四個主體與選定稿一致：完整台灣與新聞訊號、三片審查玻璃與文件、圓形儀表連接開關、三個链結存證方塊與指紋。沒有地球或試管。
- 深色模式保留淺色攝影棚卡面以呈現透明玻璃；周圍介面使用原本深色 tokens，沒有用反相濾鏡改變素材。
- P3：高解析重新生成後，玻璃折射、刻度與細節位置和選稿稍有差異；主體、鏡頭方向、色調與材質一致，屬可接受素材差異。
- 第一輪有效的同圖比對通過，沒有 P0／P1／P2 修正迭代。瀏覽器點按會自動捲動，擷取已依 DOM 位置重新對齊；此為截圖處理而非介面缺陷。

## 五項視覺檢查

| 項目 | 結果 |
| --- | --- |
| 字體與層級 | 沿用專案字體、英文展示標題、中文標題與內文層級；中英文均自然換行。 |
| 間距與構圖 | 桌面保留原雙欄與 4:5 卡框；手機採直幅、最大寬 440px，所有主體完整入鏡。 |
| 色彩與 tokens | 背景、正文與按鈕沿用 tokens；素材淺灰綠底、翠綠玻璃與選稿一致。圖內編號維持深綠字和半透明白底。 |
| 素材品質 | 四張 1122 × 1402 WebP，quality 85，共 369724 bytes；未拉伸，未用圖示或 CSS 圖形代替插圖。 |
| 文案與內容 | 保留功能標題與說明；序號、圖片與文字同步。圖片為輔助插圖，不在螢幕閱讀器重複朗讀。 |

## 互動與技術驗證

- 四張圖片在瀏覽器均 complete，naturalWidth 均為 1122，沒有破圖。
- 上一項／下一項可正反向循環，04 → 01 與 01 → 04 均實測通過。
- Enter、Space 可切換，按鈕焦點保留；將文字動畫 key 範圍縮到文字，避免按鈕重新掛載而丟失焦點。
- 圖片以 opacity 淡入淡出，不在切換時重新建立元素；沿用全站 reduced-motion 樣式。
- aria-live 提供文字更新，輪播有本地化名稱，箭頭使用既有 Lucide 圖示库。
- 手機選單、中英文與深淺色切換正常，切換設定後輪播位置保留。
- `npx eslint src/app/page.tsx`、55 項既有 Vitest 測試、`npm run build -- --webpack`（含 TypeScript 與靜態匯出）均通過。
- Impeccable detectText 輸出 `[]`；瀏覽器最後 error／warn 紀錄為 `[]`。
- 正式 WebP 隨專案靜態匯出，無執行時遠端圖片依賴。
- 已恢復原 viewport 1011 × 857，中文、淺色、第一張台灣圖，預覽停在核心優勢區塊。

## 完成清單

- [x] 四張正式插圖、原檔與提示詞均保存。
- [x] 套用輪播、完整手機比例與淡入切換。
- [x] 完成桌面、手機、深淺色、中英文及鍵盤操作檢查。
- [x] 通過 lint、測試與正式建置。
- [x] 保存來源、瀏覽器畫面与並排比對證據。

先前驗證保存在 `output/agents-accordion/design-qa.md` 與 `output/leaf-animation/design-qa.md`。
