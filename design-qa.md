# 首頁葉片動畫驗證

final result: passed

驗證日期：2026-09-06。範圍為使用者確認的「葉片飄落、定位後開合」、淺色預設／深色配色，以及既有首頁與導覽功能的整合。概念圖作為材質與構圖方向；本次並非整張概念圖的逐像素重製。

## 視覺來源與證據

- 淺色來源：`/Users/ritali/.codex/generated_images/01a072cd-c53c-7ac1-8277-4ff9a21698ed/exec-b4f324ee-df5a-47b8-b82c-925a555e8df9.png`
- 深色來源：`/Users/ritali/.codex/generated_images/01a072cd-c53c-7ac1-8277-4ff9a21698ed/exec-da2764b6-ff87-4967-aa13-2d0797239e46.png`
- 輪廓來源：`frontend/public/brand/logo-mark.png`。模型直接追蹤原始 PNG 的透明輪廓，保留 F 形切口。
- 實作網址：<http://localhost:3000/>
- 實作截圖：`output/leaf-animation/desktop-light.png`、`desktop-dark.png`、`mobile-light.png`。
- 全景同圖比對：`output/leaf-animation/comparison-light.png`。
- 葉片局部同圖比對：`output/leaf-animation/comparison-leaf.png`。
- 英文手機驗證：`output/leaf-animation/mobile-english.png`（最後一輪材質調整前；英文排版其後未更動）。

桌面 CSS viewport 與截圖均為 1672 × 941；淺色來源亦為 1672 × 941。全景比對未縮放兩張原圖，只加上標籤。局部比對將葉片區域等比例縮放到相鄰欄位，用於比較輪廓與材質，不用於判斷版面尺寸。手機 viewport／截圖為 390 × 844，截圖密度為每 CSS px 對應一個影像 px。概念圖沒有對應手機稿，因此手機以閱讀順序、完整輪廓、控制項與水平溢出檢查為準。

狀態：首頁、繁體中文、葉片完成入場。淺色截圖取開合循環中的一格；深色截圖為暫停狀態。概念圖為靜態離線渲染，沒有可比對的精確時間軸。

## 發現與修正紀錄

1. **已修正 P2：玻璃面出現三角折面。** 初版直接彎曲稀疏三角網格造成不自然的切面。改用平整透光面與獨立倒角，最終葉片局部比對可見連續表面。
2. **已修正 P2：手機動畫控制靠近既有浮動按鈕。** 將手機控制列移至葉片左下方，保留 44px 按鈕高度。最終 `mobile-light.png` 可見兩個動畫控制與右側既有操作分開。
3. **已修正 P2：玻璃邊緣過薄，開合層次不清楚。** 增加模型倒角厚度與段數、分離深綠邊緣材質、調整外側葉片開合角度。重新載入後捕捉最終桌面／手機畫面；葉片的 F 切口與多層邊緣均可辨識。
4. **P3 後續材質精修：** 即時模型的表面、反射與地面陰影比概念圖簡化，尤其沒有離線渲染的細緻焦散。這是目前可互動動畫的視覺差異，不宣稱已達概念圖的攝影級質感；可以另行精修燈光與曲面。

## 五項視覺檢查

| 面向 | 結果 |
| --- | --- |
| 字體與文字層級 | 沿用專案字體；主標題維持兩行黑／綠層級。中文與英文在手機均可完整閱讀。實際字形與概念圖生成字形有差異，屬既有品牌字體整合。 |
| 間距與構圖 | 桌面為左文字、右葉片，手機依文字、按鈕、葉片排列。1672px、390px 寬度均未出現水平溢出。保留目前專案導覽與浮動操作，因此不複製概念圖的簡化導覽。 |
| 色彩與 tokens | 畫布背景與專案淺色 `#f3f6f4`、深色 `#0f1613` 一致。深色使用較亮葉緣，F 切口與按鈕可辨識。 |
| 圖像與資產 | 使用原 logo 輪廓建立三層真實 3D 網格，沒有珍珠。葉片整體形狀與切口保留品牌辨識；材質差異如 P3 所述。 |
| 文案與功能 | 主標題使用確認的「預見風險，保障未然。」；目前專案文案為台灣事件。CTA 連至既有分析與歷史頁；語言、主題與導覽沿用專案功能。 |

## 互動與技術驗證

- 葉片入場 2.4 秒後，整體位置固定；開合週期 6 秒。三個新增測試覆蓋入場連續性、固定位置／開合週期，以及 reduced motion 靜態姿勢。
- 瀏覽器實測暫停、繼續、重播飄落；重播回到 entering，完成後進入 breathing。
- 實測暫停時切换深淺色仍保持 paused，不重置入場；恢復或重播可繼續運作。
- 實測中英文與手機選單。390px viewport 的 document 寬度為 390px。
- 點擊歷史頁後 canvas 數量由 1 變為 0，返回首頁後為 1，未重複掛載畫布。
- 最終重新載入與路由／主題測試後，瀏覽器沒有新增 error／warn（篩選時間晚於 2026-09-05T19:16:00Z，排除先前熱更新中的歷史訊息）。
- 全套單元測試 55 個通過；TypeScript 與變更檔案 ESLint 通過。
- 最後一版 `npm run build -- --webpack` 通過。預設 Turbopack 建置曾因本機子程序開啟通訊埠權限 EPERM 失敗，未修改專案預設建置命令。
- reduced motion 的姿勢經單元測試，相關 CSS 隱藏動畫控制；未變更作業系統設定來進行瀏覽器端驗證。WebGL 不可用時的原 logo fallback 已實作，未強制模擬 GPU 故障。

## 完成清單

- [x] 飄落後固定位置開合，支持暫停與重播。
- [x] 深淺色與原專案功能整合。
- [x] 桌面／手機實際瀏覽器檢查。
- [x] 最終來源與實作同圖比對。
- [x] 測試、型別、lint 與 Webpack 正式建置通過。
- [x] 恢復原本預覽尺寸，保持中文淺色動畫供使用者查看。
